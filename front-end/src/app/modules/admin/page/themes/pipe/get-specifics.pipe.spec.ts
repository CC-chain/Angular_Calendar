import { GetSpecificsPipe } from './get-specifics.pipe';

describe('GetSpecificsPipe', () => {
  it('create an instance', () => {
    const pipe = new GetSpecificsPipe();
    expect(pipe).toBeTruthy();
  });
});
