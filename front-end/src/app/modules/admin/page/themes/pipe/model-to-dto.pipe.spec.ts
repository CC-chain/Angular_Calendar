import { ModelToDtoPipe } from './model-to-dto.pipe';

describe('ModelToDtoPipe', () => {
  it('create an instance', () => {
    const pipe = new ModelToDtoPipe();
    expect(pipe).toBeTruthy();
  });
});
