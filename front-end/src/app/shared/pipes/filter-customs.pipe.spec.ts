import { FilterCustomsPipe } from './filter-customs.pipe';

describe('FilterCustomsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCustomsPipe();
    expect(pipe).toBeTruthy();
  });
});
