import { ConvertStylePipe } from './convert-style.pipe';

describe('ConvertStylePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertStylePipe();
    expect(pipe).toBeTruthy();
  });
});
