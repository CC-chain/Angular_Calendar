import { FilterWebSitesPipe } from './filter-web-sites.pipe';

describe('FilterWebSitesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterWebSitesPipe();
    expect(pipe).toBeTruthy();
  });
});
