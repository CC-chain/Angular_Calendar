import { DynamicContentPipe } from './dynamic-content.pipe';

describe('DynamicContentPipe', () => {
  it('create an instance', () => {
    const pipe = new DynamicContentPipe();
    expect(pipe).toBeTruthy();
  });
});
