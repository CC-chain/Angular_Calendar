import { GetParsersPipe } from './get-parsers.pipe';

describe('GetParsersPipe', () => {
  it('create an instance', () => {
    const pipe = new GetParsersPipe();
    expect(pipe).toBeTruthy();
  });
});
